complete -c vault -f -d "create, view, load or edit an ansible-vault" \
    -a "create view load edit" \
    -d "create: Create a new vault" \
    -d "view: View an existing vault" \
    -d "load: Load an existing vault" \
    -d "edit: Edit an existing vault"

